'use client';import{useState}from'react';
export default function TikTokPlanner(){const[length,setLength]=useState(30),[topic,setTopic]=useState('');const hook=Math.max(2,Math.round(length*.1)),cta=Math.max(3,Math.round(length*.15)),body=length-hook-cta;return <><div className="field"><label htmlFor="tiktok-video-topic">Video topic</label><input id="tiktok-video-topic" placeholder="e.g. Three easy desk setup upgrades" value={topic} onChange={e=>setTopic(e.target.value)}/></div><div className="field"><label htmlFor="tiktok-target-length">Target length</label><select id="tiktok-target-length" value={length} onChange={e=>setLength(+e.target.value)}><option value="15">15 seconds</option><option value="30">30 seconds</option><option value="60">60 seconds</option><option value="90">90 seconds</option></select></div><div className="result"><span>{topic||'Your video'} plan</span><strong>{length}-second structure</strong><p><b>0–{hook}s:</b> Hook and promise<br/><b>{hook}–{hook+body}s:</b> Main value and examples<br/><b>{hook+body}–{length}s:</b> Clear call to action</p></div></>}
