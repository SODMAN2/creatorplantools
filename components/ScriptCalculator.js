'use client';import{useState}from'react';
export default function ScriptCalculator(){const[mins,setMins]=useState(10),[wpm,setWpm]=useState(140);const words=Math.round((+mins||0)*(+wpm||0));return <><div className="field"><label>Target video length (minutes)</label><input type="number" min="0.5" step="0.5" value={mins} onChange={e=>setMins(e.target.value)}/></div><div className="field"><label>Speaking pace</label><select value={wpm} onChange={e=>setWpm(e.target.value)}><option value="110">Slow — 110 words/min</option><option value="140">Conversational — 140 words/min</option><option value="170">Fast — 170 words/min</option></select></div><div className="result"><span>Suggested script length</span><strong>{words.toLocaleString()} words</strong><span>Allow room for pauses, visuals, and transitions.</span></div></>}
