'use client';import{useState}from'react';
export default function VoiceCalculator(){const[words,setWords]=useState(1000),[wpm,setWpm]=useState(140);const seconds=Math.round((+words||0)/(+wpm||1)*60);const time=`${Math.floor(seconds/60)} min ${seconds%60} sec`;return <><div className="field"><label>Script word count</label><input type="number" min="1" value={words} onChange={e=>setWords(e.target.value)}/></div><div className="field"><label>Voiceover pace</label><select value={wpm} onChange={e=>setWpm(e.target.value)}><option value="110">Slow and deliberate — 110 WPM</option><option value="140">Natural — 140 WPM</option><option value="170">Energetic — 170 WPM</option></select></div><div className="result"><span>Estimated voiceover duration</span><strong>{time}</strong><span>Actual time may vary with pauses and emphasis.</span></div></>}
