import Link from 'next/link'; import { guideList } from './guideData';
export const metadata={title:'Creator Guides',description:'Practical guides for planning YouTube videos, TikToks, Instagram content, voiceovers, scripts, and creator workflows.',alternates:{canonical:'/guides'}};
export default function GuidesPage(){return <div className="page guides-index"><div className="wrap"><div className="page-head"><div className="eyebrow">Creator guides</div><h1>Practical guides for better content</h1><p className="muted">Clear, useful advice for planning videos, writing scripts, recording voiceovers, and building a sustainable creator workflow.</p></div><div className="guide-grid">{guideList.map((guide)=><Link className="card guide-card" href={`/guides/${guide.slug}`} key={guide.slug}><h2>{guide.title}</h2><p className="muted">{guide.description}</p><b>Read guide →</b></Link>)}</div></div></div>}
