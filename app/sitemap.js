import { guideList } from './guides/guideData';
import { toolList } from './tools/toolData';
export default function sitemap(){const base='https://creatorplantools.com';const pages=['','/tools','/guides','/about','/faq','/contact','/editorial-policy','/privacy','/terms'];return [...pages.map((path)=>({url:`${base}${path}`,changeFrequency:path===''?'weekly':'monthly',priority:path===''?1:['/tools','/guides'].includes(path)?.9:.6})),...toolList.map((tool)=>({url:`${base}${tool.href}`,changeFrequency:'monthly',priority:.8})),...guideList.map((guide)=>({url:`${base}/guides/${guide.slug}`,changeFrequency:'monthly',priority:.8}))]}
